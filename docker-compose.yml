version: "2"

services:
  nodejs:
    build: .
    command: node_modules/.bin/nodemon -L app.js
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    container_name: nodejs